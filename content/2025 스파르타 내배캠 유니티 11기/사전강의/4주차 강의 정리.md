---
title: 4주차 강의 정리
date: 2025-06-03
tags:
  - 게임개발
  - Unity
  - Csharp
description:
---
# Pixel Per Unit

- Unit : 유니티 확대하면 나오는 네모 한 칸
- Pixel Per Unit : 1 유닛 위에 픽셀을 몇 개 찍는지. => ppu가 클 수록 보이기에는 작아보임

# Instantiate

```csharp
GameObject go = Instantiate(card, this.transform);
// 생성한 인스턴스의 부모를 이 객체로 함
```


# 배열 무작위로 섞기

```csharp
using System.Linq;
...

arr = arr.OrderBy(x => Random.Range(0f, 7f)).ToArray();
// x가 0에서 7까지 순회하면서 랜덤값을 생성해냄. 그걸 바탕으로 순서를 바꿈
```


# String에 변수넣기

```csharp
$"rtan{index}"
// String 앞에 $ 표시 넣으면 string 안에서 {} 괄호에 변수 넣어서 쓸 수 있음
```

