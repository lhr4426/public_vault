---
title: 두 수 비교하기
date: 2025-01-09
tags:
  - 알고리즘
  - 코딩테스트
  - Cpp
description: 두 수 비교하기
---
[문제 링크](https://www.acmicpc.net/problem/1330)  
난이도 : 브론즈 5

# 문제 내용

두 정수 A와 B가 주어졌을 때, A와 B를 비교하는 프로그램을 작성하시오.

## 입력

첫째 줄에 A와 B가 주어진다. A와 B는 공백 한 칸으로 구분되어져 있다.

## 출력

첫째 줄에 다음 세 가지 중 하나를 출력한다.

- 첫째 줄에 다음 세 가지 중 하나를 출력한다.

- A가 B보다 큰 경우에는 '`>`'를 출력한다.
- A가 B보다 작은 경우에는 '`<`'를 출력한다.
- A와 B가 같은 경우에는 '\=='를 출력한다

# 문제 분석

한 줄에 다 들어온다!!

# 작성한 코드

```cpp
// 250109

#include <iostream>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    int a,b;
    cin >> a >> b;
    if (a > b) cout << ">";
    else if (a < b) cout << "<";
    else cout << "==";
}
```

# 우수 코드 분석

```cpp
#import<ios>
int a,b;main(){scanf("%d%d",&a,&b);puts(a>b?">":a<b?"<":"==");}
```

삼항 연산자 안에 삼항 연산자가 또 들어갈 수 있음!!!! 와우

puts(a>b?”>”:a<b?”<”:”==”)

뜯어보면..

```cpp
if (a > b) puts(”>”);
else {
	if (a < b) puts("<");
	else {
		puts("==");
	}
}
```
